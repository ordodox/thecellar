# The Cellar

The pub has a cellar, and the cellar goes down.

Take the lantern, go find the lost keg. Three floors of dark stone,
rooms and crooked corridors, and the rats that live between them. Bump
into one to fight it; watch your hearts; the way down is marked `>` —
what's at the bottom is marked `¤`.

- **Seeded runs** — every cellar is dealt from a seed. Same seed, same
  stone, same rats.
- **The descent** — one shared cellar a day. Everyone who goes down on
  the same date gets the same dungeon; the group chat is the
  leaderboard.
- **Challenge links** — beat a cellar, send it to a friend, watch them
  get eaten where you didn't.
- **Runs resume** — close the tab mid-floor and the lantern waits where
  you left it.
- **One file** — the whole game is a single index.html; it runs from
  disk if you want it to.

Arrows or WASD to move (tap beside yourself on a phone). Walking into a
rat is the attack. Death is permanent; the keg is worth it.
